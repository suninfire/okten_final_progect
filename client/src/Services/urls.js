const baseUrl = 'http://localhost:5005';

const urls = {
    auth: '/auth',
    drinker: '/drinker',
    tiding: '/tiding',
    pub: '/pub',
    response: '/response',
    user: '/user',
}

export {baseUrl,urls};
