const {Router} = require('express');

const newRouter = Router();

newRouter.get(
    '/:newId',
);

newRouter.post(
    '/',
);

newRouter.put(
    '/:newId',
);

newRouter.delete(
    '/:newId',
);

module.exports = newRouter;